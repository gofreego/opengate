package constants

const (
	// InternalRoutePrefix is the prefix for all internal routes
	InternalRoutePrefix = "/internal"

	// HealthCheckRoute is the route for the health check
	HealthCheckRoute = "/health"
)
